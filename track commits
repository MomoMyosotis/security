import requests
import schedule
import os
from datetime import datetime
import json
from datetime import datetime

#############################################################
#          REQUIREMENTS                                    #
#  1. info.JSON similar to this                            #
#     {                                                    #
#    "owner": "owner of the repo",                         #
#    "repo": "name ",                                      #
#    "LOG_FILE": "LOG_FILE.JSON"                           #
#    }                                                     #
# 2. LOG_FILE.JSON is where data will  be stored           #
############################################################


# controlla se il file esiste
def check_file(a):
    # to check the file
    if a == 'a':
        quack = 'info.JSON'
    elif a == 'm':
        quack = input("file and extension: ")
    else:
        return
    try:
        with open(quack) as file:
            data = json.load(file)
    except Exception as x:
        print("an error has occured with the file. error 01\nerror -> " + str(x))
        print("__________________________________________________________________")
        return
    else:
        return data, quack

# to check the data
def checking_data(quack, bl):
    # in case data needs to be changed
        print_dict(quack)
        change = input("wanna change it? if yes print anything\nchoice: ")
        change = bool(change)
        if change == True:
            quack["OWNER"] = input("new repo's owner USERNAME: ")
            quack["REPO"] = input("new repo's link: ")
            quack["LOG_FILE"] = input("new where to save the data: ")
            # lsc -> last seen commit
            print_dict(quack)
            save_data(quack, bl, "d")
            return quack
        else:
            print("we will work with those data then.\n")
            return quack

# saving the data if ever changed
# pru = dic - bl = file
def save_data(pru, bl, mode):
    print("saving data...")
    if mode == 'd':
        try:
            with open(bl, "w") as file:
                # indent=4 rende leggibile meglio il documento
                json.dump(pru, file, indent=4)
        except Exception as e:
            print("error 02\n error -> " + str(e))
        else:
            print("change saved!")
            return pru


    elif mode == 'c':
        try:
            with open(bl, "r") as rfile:
                existing = json.load(rfile)
        except Exception as ue:
            print("error -> 06\n error -> " + str(ue))
            existing = []

        if isinstance(existing, list):
            if isinstance(pru, list):
                gotichisa = existing + pru
            else:
                gotichisa = existing + [pru]
        else:
            if isinstance(pru, list):
                gotichisa = [existing] + pru
            else:
                gotichisa = [existing, pru]
        try:
            with open(bl, "w") as sfile:
                    # indent=4 rende leggibile meglio il documento
                    json.dump(gotichisa, sfile, indent=4)
        except Exception as e:
            print("error 03\n error -> " + str(e))
        else:
            print("data saved.")
            return gotichisa

    else:
        print("error in saving the data")
        return

# stampa dizionario
def print_dict(dname):
    print("___________________________")
    for x,y in dname.items():
        print (f"{x} -> {y}")
    print("___________________________")

# get last recorded commit
def get_lrc(nuts):

    owner = nuts["owner"]
    repo = nuts["repo"].split("/")[-1]  # Estrae solo "Vet_Clinic"

    url = f"https://api.github.com/repos/{owner}/{repo}/commits"

    print (f"trying to connecto to: {url}")
    try:
        #   requests.get() prova ad ottenere una risposta dal server con timeout di 10 secondi
        response = requests.get(url, timeout=10)

        # in caso di errore nella connessione
        response.raise_for_status()

    # in caso di errori:
    except requests.exceptions.HTTPError as e:
        print("_______________________________________________")
        return (print("error 03\n HTTP error -> " + str(e))), e

    except requests.exceptions.RequestException as o:
        print("_______________________________________________")
        return print("error 04\nerror -> " + str(o)), o

    # se per qualche allineamento astrale va tutto bene
    else:
        print ("la richiesta è andata a buon fine\nrecupero dati...\n")

        commits = response.json()
        last_commit = commits[0]
        author = last_commit['commit']['author']['name']
        date = last_commit['commit']['author']['date']
        print("\ndati recuperati con successo!\n")
        return author, date

# flow
def main():
    diz, fname = check_file('a')
    # converte diz da str a dict
    diz = dict(diz)
    # controlla file e dati
    if diz is None:
        return

    deez = checking_data(diz, fname)

    autore, data = get_lrc(deez)
    print("_______________________________________________")
    print(f"autore is {autore}")
    print(f"data is {data}")
    i = datetime.now()
    current = i.strftime("%d/%m/%Y %H:%M:%S")
    gg = [f"checked at: {current}", f"author is: {autore}", f"last commit: {data}", "_________________________________________________"]
    os.system("clear")
    aa = deez["LOG_FILE"]
    save_data(gg, aa, "c")

main()

# - myoosotis

# last line
